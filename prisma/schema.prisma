generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Pothole {
  id        Int     @id @default(autoincrement())
  latitude  Float
  longitude Float
  imageUrl  String
}